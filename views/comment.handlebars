<section class="container">
  <h3 class="gallery-name">{{comment.content}}</h3>
  {{#if loggedIn}}
  <section class="flex-row justify-center">
    {{> comment-details}}
  </section>
</section>
{{else}}
<a href="/login">You must login first to view this page</a>
{{/if}}